// Create an array that contains the name of everyone in the class and then log it to the console

let folks = ["Allistair", "Alys", "Jess", "Jonathan", "Oliver", "Paul", "Rowley", "Sid", "Sasha"]

console.log(folks);

// Now use a loop to update the code to log out each name in the folks array on a separate line

for (i = 0; i < folks.length; i += 1) {
    console.log(folks[i]);
}